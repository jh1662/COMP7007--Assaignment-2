/**
 * Represents different types of relevant input data.
 * <p>
 * Utilised by 'Interpreter' class to determine how to validate each input (data type wise).
 */
public enum InputType {
    STRING,
    INTEGER,
    DECIMAL,
    TIMESTAMP
}
